<h4><%= @product.name %></h4>
<h4><%= @product.category %></h4>
<h4><%= @product.price %></h4>
<%= link_to "Delete", "/products/#{@product.id}",
                  method: :delete,
                  data: { confirm: "Are you sure?" } %>

<%= link_to "Edit", edit_product_path(@product) %>

<%= link_to "Go Back", "/products" %>